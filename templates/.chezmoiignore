*

{{- if (eq .chezmoi.os "darwin") }}
# macOS.
!/.config/
!/.local/
!/Library/
!/.zshenv

{{- else if (eq .chezmoi.os "linux") }}
# Linux.
!/.config/
!/.local/
!/.face.icon
!/.zshenv

{{-   if (lookPath "flatpak") }}
!/.var/
{{-   end }}

{{- end }}
