*

{{- if (eq .chezmoi.os "darwin") }}
# macOS.
!/.config/
!/.local/
!/Library/

{{- else if (eq .chezmoi.os "linux") }}
# Linux.
!/.config/
!/.local/
!/.face.icon

{{-   if (lookPath "flatpak") }}
!/.var/
{{-   end }}

{{- end }}
